name: Firefox Test Execution

on: [push, pull_request]

jobs:
  firefox-test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 22
      uses: actions/setup-java@v4
      with:
        java-version: '22'
        distribution: 'temurin'
        cache: 'maven'
    
    - name: Cache Maven dependencies
      uses: actions/cache@v3
      with:
        path: ~/.m2/repository
        key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
        restore-keys: ${{ runner.os }}-m2-
    
    - name: Install Firefox
      run: |
        sudo apt-get update
        sudo apt-get install -y firefox
        
    - name: Download GeckoDriver (Linux)
      run: |
        # Remove any pre-installed versions
        sudo rm -f /usr/local/bin/geckodriver
        sudo rm -f /usr/bin/geckodriver
        wget https://github.com/mozilla/geckodriver/releases/download/v0.34.0/geckodriver-v0.34.0-linux64.tar.gz
        tar -xvzf geckodriver-v0.34.0-linux64.tar.gz
        chmod +x geckodriver
        sudo mv geckodriver /usr/local/bin/
        # Verify installation
        which geckodriver
        /usr/local/bin/geckodriver --version
    

        
    
    - name: Run FirefoxTest
      run: |
        mkdir -p test-output  # Ensure directory exists
        mvn -B test -Dtest=FirefoxTest
        ls -la test-output/
    
    - name: Upload Test Report
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: test-reports
        path: |
          test-output/
          target/surefire-reports/